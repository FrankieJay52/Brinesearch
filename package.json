{
  "name": "brinesearch",
  "private": true,
  "version": "18.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix v18 run dev",
    "verify:publish-security": "node --test scripts/private-publish-audit.test.mjs scripts/retire-legacy-service-worker.test.mjs",
    "verify": "npm run verify:publish-security && npm --prefix v18 run verify",
    "build:v18": "npm ci --prefix v18 --include=dev && npm --prefix v18 run build",
    "assemble:production": "node scripts/assemble-production.mjs",
    "prepare:pages:v18": "node scripts/prepare-v18-pages.mjs",
    "build:netlify": "npm run verify:publish-security && npm run build:v18 && npm run assemble:production",
    "build": "npm run build:netlify",
    "preview": "npm --prefix v18 run preview"
  }
}
