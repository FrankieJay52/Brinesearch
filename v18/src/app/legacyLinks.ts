const productionOrigin = "https://brinesearch.com";

export const legacyBrineSearchPaths = Object.freeze({
  dashboard: `${productionOrigin}/index.html#/`,
  fieldUpdates: `${productionOrigin}/index.html#/feed`,
  controlCenter: `${productionOrigin}/index.html#/settings/roads`,
});
