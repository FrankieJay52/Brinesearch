# Field Mark integration — V 15.0

The icon system is now integrated as a visible UI language, not only as asset substitutions. All core navigation and high-use interactive controls share the same tile geometry, scale, stroke rendering, active states, and theme behavior.
